# PWA icons

Drop `icon-192.png` and `icon-512.png` into this folder. Until you do, the
manifest references will 404 — non-fatal for development, but install the icons
before shipping. Recommended source: a 1024×1024 PNG of the Dine mark, exported
to both sizes via `sharp` or any standard image pipeline.
